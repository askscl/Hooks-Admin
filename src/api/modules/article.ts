import list from "@/api/modules/article.json"
// * 获取文章列表
export const getMenuList = () => {
    // return menuList;
    const data = list
    return data
    // return http.get<Article.ResList>(PORT1 + `/menu/list`)
}
